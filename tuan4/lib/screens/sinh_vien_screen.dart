import 'package:flutter/material.dart';

class SinhVienScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Sinh viên")),
      body: const Center(child: Text("Trang Sinh viên")),
    );
  }
}
