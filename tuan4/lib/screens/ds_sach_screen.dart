import 'package:flutter/material.dart';

class DsSachScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Danh sách Sách")),
      body: const Center(child: Text("Trang DS Sách")),
    );
  }
}
